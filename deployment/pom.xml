<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0           http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.bioid.keycloak</groupId>
    <artifactId>keycloak-bioid-extension</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>keycloak-bioid-deployment</artifactId>
  <packaging>jar</packaging>

  <name>Keycloak BioID Extension - Deployment Package</name>
  <description>Deployment package containing all modules for Keycloak installation</description>

  <dependencies>
    <!-- Include all extension modules -->
    <dependency>
      <groupId>com.bioid.keycloak</groupId>
      <artifactId>bioid-client</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.bioid.keycloak</groupId>
      <artifactId>face-credential</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.bioid.keycloak</groupId>
      <artifactId>face-enroll-action</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.bioid.keycloak</groupId>
      <artifactId>face-authenticator</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.bioid.keycloak</groupId>
      <artifactId>ui-components</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.bioid.keycloak</groupId>
      <artifactId>bws-admin-api</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- Temporarily exclude failed-auth-storage to resolve Hibernate conflicts -->
    <!--
    <dependency>
      <groupId>com.bioid.keycloak</groupId>
      <artifactId>failed-auth-storage</artifactId>
      <version>${project.version}</version>
    </dependency>
    -->

    <!-- Runtime dependencies that need to be included -->
    <dependency>
      <groupId>io.grpc</groupId>
      <artifactId>grpc-netty-shaded</artifactId>
    </dependency>
    <dependency>
      <groupId>io.grpc</groupId>
      <artifactId>grpc-protobuf</artifactId>
    </dependency>
    <dependency>
      <groupId>io.grpc</groupId>
      <artifactId>grpc-stub</artifactId>
    </dependency>
    <dependency>
      <groupId>com.google.protobuf</groupId>
      <artifactId>protobuf-java</artifactId>
    </dependency>
    <dependency>
      <groupId>javax.annotation</groupId>
      <artifactId>javax.annotation-api</artifactId>
    </dependency>

    <!-- Security dependencies -->
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
    </dependency>
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
    </dependency>

    <!-- JSON processing -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.datatype</groupId>
      <artifactId>jackson-datatype-jsr310</artifactId>
    </dependency>

    <!-- Image processing for failed auth storage -->
    <dependency>
      <groupId>org.imgscalr</groupId>
      <artifactId>imgscalr-lib</artifactId>
      <version>4.2</version>
    </dependency>

    <!-- Monitoring -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-core</artifactId>
    </dependency>


  </dependencies>

  <build>
    <plugins>
      <!-- Create a fat JAR with all dependencies -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <goals>
              <goal>shade</goal>
            </goals>
            <phase>package</phase>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <shadedArtifactAttached>false</shadedArtifactAttached>
              <finalName>keycloak-bioid-extension-${project.version}</finalName>

              <!-- Relocate packages to avoid conflicts -->
              <relocations>
                <!--
                  NOTE: Relocations for com.google.protobuf and io.grpc were removed because
                  shading those generated classes causes an ASM IndexOutOfBoundsException while
                  processing com/bioid/services/Bws$VideoLivenessDetectionRequest.
                  Keeping the original package names avoids the build failure and still bundles the
                  dependencies inside the fat JAR.
                -->
                <!-- Remove netty relocation since grpc-netty-shaded already handles this -->
                <relocation>
                  <pattern>com.fasterxml.jackson</pattern>
                  <shadedPattern>com.bioid.shaded.jackson</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>io.jsonwebtoken</pattern>
                  <shadedPattern>com.bioid.shaded.jwt</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>org.bouncycastle</pattern>
                  <shadedPattern>com.bioid.shaded.bouncycastle</shadedPattern>
                </relocation>
              </relocations>

              <!-- Include/exclude patterns -->
              <filters>
                <!-- Exclude JAXB artifacts completely -->
                <filter>
                  <artifact>jakarta.xml.bind:jakarta.xml.bind-api</artifact>
                  <excludes>
                    <exclude>**</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>org.glassfish.jaxb:jaxb-runtime</artifact>
                  <excludes>
                    <exclude>**</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>org.glassfish.jaxb:jaxb-core</artifact>
                  <excludes>
                    <exclude>**</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>org.glassfish.jaxb:txw2</artifact>
                  <excludes>
                    <exclude>**</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>org.eclipse.angus:angus-activation</artifact>
                  <excludes>
                    <exclude>**</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>com.sun.istack:istack-commons-runtime</artifact>
                  <excludes>
                    <exclude>**</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                    <exclude>META-INF/MANIFEST.MF</exclude>
                    <exclude>module-info.class</exclude>
                    <!-- Exclude SLF4J implementations to prevent conflicts with Keycloak -->
                    <exclude>org/slf4j/impl/**</exclude>
                    <exclude>META-INF/services/org.slf4j.spi.SLF4JServiceProvider</exclude>
                    <!-- Exclude Hibernate C3P0 provider to prevent conflicts -->
                    <exclude>org/hibernate/c3p0/**</exclude>
                    <exclude>META-INF/services/org.hibernate.boot.registry.selector.StrategyRegistrationProvider</exclude>
                    <!-- Exclude conflicting JAXB service files -->
                    <exclude>META-INF/services/jakarta.xml.bind.JAXBContextFactory</exclude>
                    <!-- Exclude ALL JAXB classes to prevent classloader conflicts -->
                    <exclude>jakarta/xml/bind/**</exclude>
                    <exclude>org/glassfish/jaxb/**</exclude>
                    <exclude>com/sun/xml/bind/**</exclude>
                    <exclude>com/sun/istack/**</exclude>
                    <exclude>org/eclipse/angus/**</exclude>
                    <!-- Exclude Hibernate classes to prevent classloader conflicts -->
                    <exclude>org/hibernate/**</exclude>
                    <exclude>META-INF/services/org.hibernate.*</exclude>
                    <!-- Exclude JPA classes that might conflict -->
                    <exclude>jakarta/persistence/**</exclude>
                    <exclude>javax/persistence/**</exclude>
                    <exclude>META-INF/persistence.xml</exclude>
                  </excludes>
                </filter>
              </filters>

              <!-- Merge service files -->
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <manifestEntries>
                    <Implementation-Title>Keycloak BioID Face Recognition Extension</Implementation-Title>
                    <Implementation-Version>${project.version}</Implementation-Version>
                    <Implementation-Vendor>BioID</Implementation-Vendor>
                    <Built-By>${user.name}</Built-By>
                    <Build-Jdk>${java.version}</Build-Jdk>
                    <Build-Time>${maven.build.timestamp}</Build-Time>
                    <Keycloak-Version>${keycloak.version}</Keycloak-Version>
                  </manifestEntries>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Create distribution archive -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>3.7.1</version>
        <configuration>
          <descriptors>
            <descriptor>src/assembly/distribution.xml</descriptor>
          </descriptors>
          <finalName>keycloak-bioid-extension-${project.version}</finalName>
          <appendAssemblyId>false</appendAssemblyId>
        </configuration>
        <executions>
          <execution>
            <id>make-assembly</id>
            <goals>
              <goal>single</goal>
            </goals>
            <phase>package</phase>
          </execution>
        </executions>
      </plugin>

      <!-- Generate installation documentation -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <id>copy-documentation</id>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <phase>prepare-package</phase>
            <configuration>
              <outputDirectory>${project.build.directory}/docs</outputDirectory>
              <resources>
                <resource>
                  <directory>src/main/docs</directory>
                  <filtering>true</filtering>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
