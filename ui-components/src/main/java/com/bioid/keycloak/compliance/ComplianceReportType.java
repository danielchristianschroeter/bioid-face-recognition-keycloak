package com.bioid.keycloak.compliance;

/** Defines the types of compliance reports that can be generated. */
public enum ComplianceReportType {
  /** JSON format report. */
  JSON,

  /** CSV format report. */
  CSV,

  /** PDF format report. */
  PDF
}
